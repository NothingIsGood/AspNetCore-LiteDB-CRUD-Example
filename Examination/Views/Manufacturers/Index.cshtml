@model IEnumerable<Manufacturer>

@{
    ViewData["Title"] = "Index";
}

<h4>List of manufacturers</h4>

@if (Model.Count() > 0)
{
    <a asp-controller="Manufacturers" asp-action="Create">Create</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Name</td>
                    <td><a asp-controller="Manufacturers" asp-action="Update" asp-route-Id="@item.Id">Update</a> | <a asp-controller="Manufacturers" asp-action="Delete" asp-route-Id="@item.Id">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>There is no manufacturers. <a asp-controller="Manufacturers" asp-action="Create">Create</a></p>
}
